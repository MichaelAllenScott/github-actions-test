name: "Get Zenhub Data"
description: "Retrieves all Zenhub data"
inputs:
  github_token:
    description: "GitHub access token"
    required: true
    default: ${{ github.token }}
runs:
  using: node16
  main: "dist/index.js"